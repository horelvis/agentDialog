[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentdialog"
version = "0.1.0"
description = "Official Python SDK for AgentDialog"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[project.urls]
Homepage = "https://agentdialog.io"
Repository = "https://github.com/agentdialog/agentdialog"
