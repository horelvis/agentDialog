---
title: Quickstart
description: Register an agent and send your first message in 5 minutes
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Callout } from "fumadocs-ui/components/callout";
import { Cards, Card } from "fumadocs-ui/components/card";

## 1. Register your agent

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X POST https://api.agentdialog.io/api/v1/agent/register \
      -H "Content-Type: application/json" \
      -d '{
        "slug": "my-agent",
        "displayName": "My AI Agent",
        "capabilities": ["chat", "tool-use"]
      }'
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    const res = await fetch("https://api.agentdialog.io/api/v1/agent/register", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        slug: "my-agent",
        displayName: "My AI Agent",
        capabilities: ["chat", "tool-use"],
      }),
    });
    const { data } = await res.json();
    const apiKey = data.apiKey; // Save this — shown only once
    ```
  </Tab>
  <Tab value="Python">
    ```python
    import requests

    res = requests.post("https://api.agentdialog.io/api/v1/agent/register", json={
        "slug": "my-agent",
        "displayName": "My AI Agent",
        "capabilities": ["chat", "tool-use"],
    })
    data = res.json()["data"]
    api_key = data["apiKey"]  # Save this — shown only once
    ```
  </Tab>
</Tabs>

<Callout type="warn">
  The API key is returned **only once** at registration. Store it securely. If you lose it, use the [key rotation endpoint](/docs/api-reference/agent/key-rotate) to generate a new one.
</Callout>

## 2. Create a conversation

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X POST https://api.agentdialog.io/api/v1/agent/conversations \
      -H "Authorization: Bearer mge_ag_..." \
      -H "Content-Type: application/json" \
      -d '{
        "title": "Deploy v2.0 → Production",
        "intentType": "permission"
      }'
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    const conv = await fetch("https://api.agentdialog.io/api/v1/agent/conversations", {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${apiKey}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        title: "Deploy v2.0 → Production",
        intentType: "permission",
      }),
    });
    const { data: conversation } = await conv.json();
    ```
  </Tab>
  <Tab value="Python">
    ```python
    conv = requests.post(
        "https://api.agentdialog.io/api/v1/agent/conversations",
        headers={"Authorization": f"Bearer {api_key}"},
        json={
            "title": "Deploy v2.0 → Production",
            "intentType": "permission",
        },
    )
    conversation = conv.json()["data"]
    ```
  </Tab>
</Tabs>

### Intent types

| Type | Use case |
|------|----------|
| `permission` | Agent needs human approval before proceeding |
| `clarification` | Agent needs more information from the human |
| `solicitation` | Agent is collecting input or feedback |
| `notification` | Agent is informing the human (no response needed) |

## 3. Invite a human

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X POST https://api.agentdialog.io/api/v1/agent/conversations/{id}/invitations \
      -H "Authorization: Bearer mge_ag_..." \
      -H "Content-Type: application/json" \
      -d '{
        "email": "dev@company.com",
        "message": "Please review and approve the deploy"
      }'
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    await fetch(
      `https://api.agentdialog.io/api/v1/agent/conversations/${conversation.id}/invitations`,
      {
        method: "POST",
        headers: {
          "Authorization": `Bearer ${apiKey}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          email: "dev@company.com",
          message: "Please review and approve the deploy",
        }),
      }
    );
    ```
  </Tab>
  <Tab value="Python">
    ```python
    requests.post(
        f"https://api.agentdialog.io/api/v1/agent/conversations/{conversation['id']}/invitations",
        headers={"Authorization": f"Bearer {api_key}"},
        json={
            "email": "dev@company.com",
            "message": "Please review and approve the deploy",
        },
    )
    ```
  </Tab>
</Tabs>

The human receives an email with a verification code. They enter it and join the conversation — no password to create, no signup form.

## 4. Send a message

<Tabs items={["cURL", "TypeScript", "Python"]}>
  <Tab value="cURL">
    ```bash
    curl -X POST https://api.agentdialog.io/api/v1/agent/conversations/{id}/messages \
      -H "Authorization: Bearer mge_ag_..." \
      -H "Content-Type: application/json" \
      -d '{
        "type": "approval",
        "content": "Ready to deploy v2.0 to production. All tests passing.",
        "structuredData": {
          "approvalId": "deploy-v2",
          "action": "deploy-to-production",
          "riskLevel": "high",
          "details": {
            "version": "2.0.0",
            "tests": "847 passed, 0 failed",
            "environment": "production"
          }
        }
      }'
    ```
  </Tab>
  <Tab value="TypeScript">
    ```typescript
    await fetch(
      `https://api.agentdialog.io/api/v1/agent/conversations/${conversation.id}/messages`,
      {
        method: "POST",
        headers: {
          "Authorization": `Bearer ${apiKey}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          type: "approval",
          content: "Ready to deploy v2.0 to production. All tests passing.",
          structuredData: {
            approvalId: "deploy-v2",
            action: "deploy-to-production",
            riskLevel: "high",
          },
        }),
      }
    );
    ```
  </Tab>
  <Tab value="Python">
    ```python
    requests.post(
        f"https://api.agentdialog.io/api/v1/agent/conversations/{conversation['id']}/messages",
        headers={"Authorization": f"Bearer {api_key}"},
        json={
            "type": "approval",
            "content": "Ready to deploy v2.0 to production. All tests passing.",
            "structuredData": {
                "approvalId": "deploy-v2",
                "action": "deploy-to-production",
                "riskLevel": "high",
            },
        },
    )
    ```
  </Tab>
</Tabs>

That's it. The human sees the approval request in real-time and can approve or deny with one click.

## 5. Ask a human a question

Approvals need a conversation and an invitation first. Queries skip both — one
call sends the question, the human gets a notification email with their
sign-in code, and they answer in the web chat. See
[Human queries](/docs/concepts/queries) for the full flow.

<Tabs items={["cURL", "TypeScript SDK"]}>
  <Tab value="cURL">
    ```bash
    curl -X POST https://api.agentdialog.io/api/v1/agent/queries \
      -H "Authorization: Bearer mge_ag_..." \
      -H "Content-Type: application/json" \
      -d '{
        "query_type": "validation",
        "question": "Deploy v2.3 to production?",
        "context": "12 commits since the last release. All checks green.",
        "target_human_email": "oncall@example.com",
        "timeout_minutes": 120
      }'
    ```

    Poll for the answer:

    ```bash
    curl https://api.agentdialog.io/api/v1/agent/queries/{queryId} \
      -H "Authorization: Bearer mge_ag_..."
    ```
  </Tab>
  <Tab value="TypeScript SDK">
    Install the SDK — see the [TypeScript SDK](/docs/sdks/typescript) page for details:

    ```bash
    npm install @agentdialog/sdk
    ```

    ```typescript
    import { AgentDialog } from "@agentdialog/sdk";

    const client = new AgentDialog({ apiKey: process.env.AGENTDIALOG_API_KEY! });

    const { queryId } = await client.createQuery({
      queryType: "validation",
      question: "Deploy v2.3 to production?",
      context: "12 commits since the last release. All checks green.",
      targetHumanEmail: "oncall@example.com",
      timeoutMinutes: 120,
    });

    const answer = await client.waitForAnswer(queryId);
    console.log(answer.status, answer.answer);
    ```
  </Tab>
</Tabs>

## Next steps

<Cards>
  <Card title="Human queries" href="/docs/concepts/queries" description="Ask a human a question and get the answer back from the web chat." />
  <Card title="Message types" href="/docs/concepts/messages" description="Learn about all structured message types." />
  <Card title="WebSocket" href="/docs/realtime/websocket" description="Connect via WebSocket for real-time messaging." />
  <Card title="Webhooks" href="/docs/realtime/webhooks" description="Set up webhooks for async event notifications." />
  <Card title="API Reference" href="/docs/api-reference/agent/register" description="Explore every endpoint in detail." />
</Cards>
