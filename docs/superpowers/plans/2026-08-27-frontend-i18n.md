# La interfaz en tres idiomas — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** que la landing, `/q/:token` y el chat se pinten en inglés, español o
catalán, con la precedencia selector > declarado > navegador, sin tocar el
backend.

**Architecture:** un resolutor puro decide el idioma y react-i18next lo pinta.
Los catálogos son cuatro namespaces (`common`, `landing`, `query`, `chat`), un
fichero por idioma y namespace, cargados con `import()` dinámico. El idioma
elegido a mano vive en `localStorage`; el declarado por el agente llega en la
respuesta de `/q/:token` y **no se guarda**. Dos pruebas puras en la suite de la
raíz —precedencia y paridad de claves— porque es lo único que CI ejecuta.

**Tech Stack:** React 19, Vite 6, TypeScript 5.9, `i18next` + `react-i18next` +
`i18next-resources-to-backend`, `eslint-plugin-i18next` como red de seguridad,
Bun para las pruebas.

**Spec:** `docs/superpowers/specs/2026-08-27-frontend-i18n-design.md`

## Global Constraints

- **El código, los comentarios y los mensajes de commit, en inglés.** Lo que
  cambia con este trabajo es el **texto visible de `web/`**, que deja de vivir
  en el JSX y pasa al catálogo. Una cadena visible nueva escrita a mano en un
  componente es, desde aquí, un fallo.
- **El catálogo es cerrado: `en`, `es`, `ca`.** `en` es la fuente; `es` y `ca`
  se traducen desde él. El euskera no entra.
- **Cero backend.** Ninguna tarea toca `src/`, `migrations/` ni la base de datos.
  Si alguna parece necesitarlo, para y pregunta.
- **Los ficheros que importa la suite de la raíz usan rutas relativas, nunca el
  alias `@/`.** El alias existe en los dos `tsconfig` y apunta a sitios
  distintos: en `web/` a `web/src/`, en la raíz a `src/`, que es el backend. Un
  `@/lib/storage` ejecutado desde la raíz no falla: resuelve al backend. Afecta a
  `web/src/i18n/languages.ts`, `web/src/i18n/resolve.ts`, `web/src/lib/storage.ts`
  y a todos los ficheros de `web/src/i18n/catalogues/`. El resto de `web/` sigue
  usando `@/` con normalidad.
- **Tono de las traducciones: tuteo**, en español y en catalán. Es lo que usa el
  original en inglés y lo que espera este producto.
- **No se traducen los términos del producto**: AgentDialog, MCP, API key,
  webhook, SDK, token. Tampoco los nombres de los idiomas en el selector, que van
  en su propio idioma (English, Español, Català).
- **La suite se nombra entera o no se nombra:** `bun test tests/unit tests/integration`.
  Un `bun test` a secas desde la raíz arrastra las pruebas del SDK y falla en un
  clon limpio.
- **En `web/` se instala y se comprueba desde `web/`:** `bun install`,
  `bunx tsc -b`, `bunx eslint .`.

---

## Estructura de ficheros

**Se crean:**

| Fichero | Responsabilidad |
|---|---|
| `web/src/lib/storage.ts` | `StorageLike`, la sonda de almacenamiento bloqueado y el respaldo en memoria. Sale de `attribution.ts`, que ya lo tenía |
| `web/src/i18n/languages.ts` | El catálogo cerrado, los `localeTag`, los nombres de idioma y la normalización de una etiqueta BCP-47 |
| `web/src/i18n/resolve.ts` | `resolveLanguage` y la lectura/escritura del idioma guardado. Puro, sin React ni i18next |
| `web/src/i18n/index.ts` | Arranque de i18next, `declare module`, `changeLanguage`, `useLanguage` |
| `web/src/i18n/catalogues/{en,es,ca}/{common,landing,query,chat}.ts` | Los doce ficheros de mensajes |
| `web/src/i18n/catalogues/{en,es,ca}/index.ts` | Junta los cuatro namespaces de un idioma. Solo lo usan el tipado y la prueba de paridad |
| `web/src/components/i18n/LanguageSelector.tsx` | El selector |
| `tests/unit/web-language-resolution.test.ts` | La tabla de precedencia |
| `tests/unit/web-catalogue-parity.test.ts` | `es` y `ca` tienen exactamente las claves de `en` |

**Se modifican:** `web/src/lib/attribution.ts`, `web/src/lib/formatters.ts`,
`web/src/main.tsx`, `web/src/App.tsx`, `web/index.html`,
`web/src/components/layout/{Footer,Navbar,Sidebar}.tsx`, los once componentes de
`web/src/components/landing/`, `web/src/pages/*.tsx`,
`web/src/components/queries/*.tsx`, `web/src/components/answer/*.tsx`,
`web/src/components/chat/**`, `web/src/components/{auth,invitations,profile,ui}/*.tsx`,
`web/package.json`, `web/eslint.config.js`, `.github/workflows/ci.yml`,
`docs-site/content/docs/roadmap.mdx`, `CLAUDE.md`.

---

## Task 1: El almacenamiento y el resolutor

Lo primero porque no depende de nada y porque es lo único que CI va a ejecutar.
Sin React, sin i18next y sin DOM: se prueba desde la raíz con Bun.

**Files:**
- Create: `web/src/lib/storage.ts`
- Create: `web/src/i18n/languages.ts`
- Create: `web/src/i18n/resolve.ts`
- Modify: `web/src/lib/attribution.ts` (quitarle `StorageLike` y `browserStorage`)
- Modify: `web/src/components/landing/Hero.tsx:3` (de dónde importa `browserStorage`)
- Test: `tests/unit/web-language-resolution.test.ts`

**Interfaces:**
- Produces:
  - `SUPPORTED_LANGUAGES: readonly ["en","es","ca"]`, `type Language`,
    `DEFAULT_LANGUAGE: Language`, `NAMESPACES: readonly ["common","landing","query","chat"]`,
    `type Namespace`, `isSupported(v: unknown): v is Language`,
    `localeTag(l: Language): string`, `narrow(tag: string | null | undefined): Language | null`,
    `LANGUAGE_NAMES: Record<Language, string>`
  - `resolveLanguage(sources: LanguageSources): Language` con
    `interface LanguageSources { stored?: string | null; declared?: string | null; navigator?: readonly string[] | null }`
  - `readStoredLanguage(storage: StorageLike): Language | null`,
    `writeStoredLanguage(storage: StorageLike, language: Language): void`,
    `LANGUAGE_KEY: string`
  - `interface StorageLike { getItem(k: string): string | null; setItem(k: string, v: string): void }`,
    `sessionStore(): StorageLike`, `persistentStore(): StorageLike`

- [ ] **Paso 1: escribe la prueba que falla**

Crea `tests/unit/web-language-resolution.test.ts`. Fíjate en las rutas: relativas
y hacia `web/`, porque esta prueba vive en la raíz y el alias `@/` de aquí apunta
al backend.

```ts
import { describe, expect, test } from "bun:test";
import {
  LANGUAGE_KEY,
  readStoredLanguage,
  resolveLanguage,
  writeStoredLanguage,
} from "../../web/src/i18n/resolve";
import { localeTag, narrow } from "../../web/src/i18n/languages";
import type { StorageLike } from "../../web/src/lib/storage";

/** A storage we control, so the test never touches a real browser. */
function fakeStorage(initial: Record<string, string> = {}): StorageLike {
  const store = { ...initial };
  return {
    getItem: (key) => (key in store ? store[key] : null),
    setItem: (key, value) => {
      store[key] = value;
    },
  };
}

/** The Safari case: touching storage throws rather than returning null. */
const hostileStorage: StorageLike = {
  getItem() {
    throw new Error("The operation is insecure.");
  },
  setItem() {
    throw new Error("The operation is insecure.");
  },
};

describe("resolveLanguage", () => {
  test("an explicit choice beats everything else", () => {
    expect(
      resolveLanguage({ stored: "es", declared: "ca", navigator: ["en-GB"] }),
    ).toBe("es");
  });

  test("what the agent declared beats the browser", () => {
    // The office computer in English, and an agent that knows this person
    // reads Catalan. This is the case the whole precedence rule exists for.
    expect(resolveLanguage({ declared: "ca", navigator: ["en-GB", "en"] })).toBe("ca");
  });

  test("the browser decides when nothing else does", () => {
    expect(resolveLanguage({ navigator: ["es-MX", "en"] })).toBe("es");
  });

  test("the browser's own order is respected, skipping what we do not have", () => {
    expect(resolveLanguage({ navigator: ["fr-FR", "de", "ca-ES", "es"] })).toBe("ca");
  });

  test("region is dropped, case is ignored", () => {
    expect(resolveLanguage({ stored: "ca-ES" })).toBe("ca");
    expect(resolveLanguage({ stored: "ES-es" })).toBe("es");
    expect(resolveLanguage({ stored: "EN-GB" })).toBe("en");
  });

  test("a value outside the catalogue is ignored, not defaulted on", () => {
    // Basque is a real request we have not shipped. A stored `eu` must fall
    // through to the next source, not short-circuit to English.
    expect(resolveLanguage({ stored: "eu", declared: "es" })).toBe("es");
    expect(resolveLanguage({ stored: "", declared: null, navigator: ["ca"] })).toBe("ca");
  });

  test("nothing at all is English", () => {
    expect(resolveLanguage({})).toBe("en");
    expect(resolveLanguage({ stored: null, declared: null, navigator: [] })).toBe("en");
  });
});

describe("the stored choice", () => {
  test("round-trips through storage", () => {
    const storage = fakeStorage();
    writeStoredLanguage(storage, "ca");
    expect(storage.getItem(LANGUAGE_KEY)).toBe("ca");
    expect(readStoredLanguage(storage)).toBe("ca");
  });

  test("a junk value reads as nothing stored", () => {
    expect(readStoredLanguage(fakeStorage({ [LANGUAGE_KEY]: "eu" }))).toBeNull();
  });

  test("storage that throws is not a crash", () => {
    // Losing the language must never take down the page somebody opened to
    // answer a question.
    expect(readStoredLanguage(hostileStorage)).toBeNull();
    expect(() => writeStoredLanguage(hostileStorage, "es")).not.toThrow();
  });
});

describe("narrow and localeTag", () => {
  test("narrow keeps only what we can render", () => {
    expect(narrow("ca-ES")).toBe("ca");
    expect(narrow("pt-BR")).toBeNull();
    expect(narrow(undefined)).toBeNull();
    expect(narrow("  es  ")).toBe("es");
  });

  test("localeTag is what Intl gets handed", () => {
    expect(localeTag("en")).toBe("en-US");
    expect(localeTag("es")).toBe("es-ES");
    expect(localeTag("ca")).toBe("ca-ES");
  });
});
```

- [ ] **Paso 2: ejecútala y comprueba que falla**

```bash
bun test tests/unit/web-language-resolution.test.ts
```

Espera: falla al resolver los módulos, `Cannot find module '../../web/src/i18n/resolve'`.

- [ ] **Paso 3: saca el almacenamiento a su propio fichero**

`web/src/lib/attribution.ts` ya tiene la sonda que detecta el almacenamiento
bloqueado y el respaldo en memoria; lo que falta es la variante persistente. Se
mueve el trozo, no se duplica.

Crea `web/src/lib/storage.ts`:

```ts
/**
 * Browser storage that never throws. Private windows and blocked site data make
 * the accessor itself throw — not the read, the *access* — so every entry point
 * probes first and falls back to memory. Nothing here is important enough to
 * take a page down over.
 */

/** A `Storage`, narrowed to what we use, so tests can hand us a fake. */
export interface StorageLike {
  getItem(key: string): string | null;
  setItem(key: string, value: string): void;
}

function createMemoryStore(): StorageLike {
  const data: Record<string, string> = {};
  return {
    getItem: (key) => (key in data ? data[key] : null),
    setItem: (key, value) => {
      data[key] = value;
    },
  };
}

// One fallback per kind, not shared — otherwise blocking both real stores
// would quietly merge session and persistent state into a single bucket.
const sessionFallback = createMemoryStore();
const persistentFallback = createMemoryStore();

const PROBE_KEY = "agentdialog:probe";

/**
 * `get` is called inside the try because reading the property itself —
 * `globalThis.localStorage`, not just calling a method on it — is what
 * throws in WebKit when site data is blocked.
 */
function usable(get: () => Storage | undefined): Storage | null {
  try {
    const store = get();
    if (!store) return null;
    store.getItem(PROBE_KEY); // throws when site data is blocked
    return store;
  } catch {
    return null;
  }
}

/** Lives for this tab only. Where attribution goes. */
export function sessionStore(): StorageLike {
  return usable(() => globalThis.sessionStorage) ?? sessionFallback;
}

/** Survives closing the tab. Where a deliberate language choice goes. */
export function persistentStore(): StorageLike {
  return usable(() => globalThis.localStorage) ?? persistentFallback;
}
```

Ojo con lo obvio que parece el borrador original: el acceso a la propiedad
(`globalThis.localStorage`) debe ir *dentro* del `try`, no solo la llamada al
método. En WebKit con datos de sitio bloqueados, leer la propiedad en sí ya
lanza `SecurityError` antes de llamar a ningún método — sacar la lectura fuera
del `try` deja el error escapar sin capturar.

En `web/src/lib/attribution.ts`: borra la interfaz `StorageLike`, la constante
`fallbackStore`, la función `browserStorage` y el comentario que la acompaña.
Sustituye por un import y un alias, para no romper a quien ya llamaba
`browserStorage`:

```ts
import { sessionStore, type StorageLike } from "./storage";

export type { StorageLike };

/** Attribution is a per-visit thing, so it lives in session storage. */
export const browserStorage = sessionStore;
```

- [ ] **Paso 4: escribe el catálogo de idiomas**

Crea `web/src/i18n/languages.ts`. Rutas relativas y nada de DOM: la suite de la
raíz lo importa.

```ts
/**
 * The closed catalogue. Adding a language means adding a value here and four
 * files of messages — that cost is the point: supporting a language means
 * having it written and reviewed by somebody who speaks it.
 */
export const SUPPORTED_LANGUAGES = ["en", "es", "ca"] as const;
export type Language = (typeof SUPPORTED_LANGUAGES)[number];

export const DEFAULT_LANGUAGE: Language = "en";

export const NAMESPACES = ["common", "landing", "query", "chat"] as const;
export type Namespace = (typeof NAMESPACES)[number];

/** What Intl gets handed. Mirrors LOCALE_TAGS in src/i18n/index.ts. */
const LOCALE_TAGS: Record<Language, string> = {
  en: "en-US",
  es: "es-ES",
  ca: "ca-ES",
};

/**
 * Autonyms: a language names itself the same way whatever the interface is set
 * to, so these are never translated and never live in a catalogue.
 */
export const LANGUAGE_NAMES: Record<Language, string> = {
  en: "English",
  es: "Español",
  ca: "Català",
};

export function isSupported(value: unknown): value is Language {
  return typeof value === "string" && (SUPPORTED_LANGUAGES as readonly string[]).includes(value);
}

export function localeTag(language: Language): string {
  return LOCALE_TAGS[language];
}

/**
 * A BCP-47 tag narrowed to what we can render, or null. Region is dropped —
 * ca-ES is Catalan and es-MX is Spanish — and anything outside the catalogue
 * is null rather than English, so the caller can try its next source instead
 * of stopping here.
 */
export function narrow(tag: string | null | undefined): Language | null {
  if (!tag) return null;
  const base = tag.trim().toLowerCase().split("-")[0];
  return isSupported(base) ? base : null;
}
```

- [ ] **Paso 5: escribe el resolutor**

Crea `web/src/i18n/resolve.ts`:

```ts
import { DEFAULT_LANGUAGE, isSupported, narrow, type Language } from "./languages";
import type { StorageLike } from "../lib/storage";

export const LANGUAGE_KEY = "agentdialog:language";

export interface LanguageSources {
  /** What this person chose by hand, on this device. */
  stored?: string | null;
  /** What the agent declared on the query. Only /q/:token has one. */
  declared?: string | null;
  /** navigator.languages, in the browser's own order of preference. */
  navigator?: readonly string[] | null;
}

/**
 * Precedence: chosen > declared > browser > English.
 *
 * The choice comes first because it is the only signal somebody gave on
 * purpose, and it is the safety net for a wrong declaration — which will
 * happen. The declaration beats the browser because the browser belongs to the
 * device: the case that decided this is the shared office computer in English,
 * where the agent knew perfectly well that this person reads Catalan.
 */
export function resolveLanguage(sources: LanguageSources): Language {
  const chosen = narrow(sources.stored);
  if (chosen) return chosen;

  const declared = narrow(sources.declared);
  if (declared) return declared;

  for (const tag of sources.navigator ?? []) {
    const fromBrowser = narrow(tag);
    if (fromBrowser) return fromBrowser;
  }

  return DEFAULT_LANGUAGE;
}

export function readStoredLanguage(storage: StorageLike): Language | null {
  let raw: string | null;
  try {
    raw = storage.getItem(LANGUAGE_KEY);
  } catch {
    return null;
  }
  return isSupported(raw) ? raw : null;
}

export function writeStoredLanguage(storage: StorageLike, language: Language): void {
  try {
    storage.setItem(LANGUAGE_KEY, language);
  } catch {
    // No storage. The choice still holds for this page view, which is the part
    // that matters right now.
  }
}
```

- [ ] **Paso 6: arregla el import de `Hero.tsx`**

`web/src/components/landing/Hero.tsx:3` sigue importando `browserStorage` de
`@/lib/attribution`, que ahora es un alias reexportado — funciona, pero apunta al
sitio equivocado. Cámbialo:

```tsx
import { rememberAttribution } from "@/lib/attribution";
import { sessionStore } from "@/lib/storage";
```

y en el `useEffect`, `rememberAttribution(window.location.search, sessionStore())`.

- [ ] **Paso 7: ejecuta las pruebas y comprueba que pasan**

```bash
bun test tests/unit/web-language-resolution.test.ts
bun test web/src/lib/attribution.test.ts
cd web && bunx tsc -b && cd ..
```

Espera: las tres verdes. La de atribución existía ya y no la corre CI; se ejecuta
a mano aquí porque esta tarea le ha movido el suelo.

- [ ] **Paso 8: commit**

```bash
git add web/src/lib/storage.ts web/src/i18n/languages.ts web/src/i18n/resolve.ts \
  web/src/lib/attribution.ts web/src/components/landing/Hero.tsx \
  tests/unit/web-language-resolution.test.ts
git commit -m "Decide the interface language before anything renders it"
```

---

## Task 2: i18next, el catálogo `common` y `<html lang>`

Al final de esta tarea la aplicación arranca en el idioma resuelto, `<html lang>`
lo refleja, y la prueba de paridad existe y está verde — vacía de contenido pero
lista para crecer con cada tarea siguiente.

**Files:**
- Modify: `web/package.json` (tres dependencias)
- Create: `web/src/i18n/catalogues/{en,es,ca}/common.ts`
- Create: `web/src/i18n/catalogues/{en,es,ca}/index.ts`
- Create: `web/src/i18n/index.ts`
- Modify: `web/src/main.tsx`
- Modify: `web/src/App.tsx`
- Test: `tests/unit/web-catalogue-parity.test.ts`

**Interfaces:**
- Consumes: de la tarea 1, `resolveLanguage`, `readStoredLanguage`,
  `writeStoredLanguage`, `persistentStore`, `DEFAULT_LANGUAGE`, `isSupported`,
  `type Language`
- Produces:
  - `initI18n(): Promise<void>` — a llamar una vez, antes de pintar
  - `changeLanguage(language: Language, opts: { persist: boolean }): Promise<void>`
  - `useLanguage(): Language` — el idioma activo, para lo que no es texto
  - Los catálogos por defecto de cada fichero, y `catalogues/<lang>/index.ts` con
    `{ common, landing, query, chat }`

- [ ] **Paso 1: escribe la prueba de paridad, que falla**

Crea `tests/unit/web-catalogue-parity.test.ts`. Importa los índices por idioma —
nunca `web/src/i18n/index.ts`, que arranca i18next y necesita un navegador.

```ts
import { describe, expect, test } from "bun:test";
import en from "../../web/src/i18n/catalogues/en";
import es from "../../web/src/i18n/catalogues/es";
import ca from "../../web/src/i18n/catalogues/ca";

/** Every leaf, as a dotted path, so two catalogues can be compared as sets. */
function paths(value: unknown, prefix = ""): string[] {
  if (typeof value !== "object" || value === null) return [prefix];
  return Object.entries(value).flatMap(([key, child]) =>
    paths(child, prefix ? `${prefix}.${key}` : key),
  );
}

/** Every leaf's value, for the emptiness check. */
function leaves(value: unknown): unknown[] {
  if (typeof value !== "object" || value === null) return [value];
  return Object.values(value).flatMap(leaves);
}

const TRANSLATIONS = [
  ["es", es],
  ["ca", ca],
] as const;

describe("catalogue parity", () => {
  // With 400 strings per language this is the only thing standing between a
  // half-finished translation and a page that silently falls back to English.
  for (const [name, catalogue] of TRANSLATIONS) {
    test(`${name} has exactly the keys of en`, () => {
      const expected = paths(en).sort();
      const actual = paths(catalogue).sort();

      expect(actual.filter((k) => !expected.includes(k))).toEqual([]); // extra
      expect(expected.filter((k) => !actual.includes(k))).toEqual([]); // missing
    });
  }

  for (const [name, catalogue] of [["en", en], ...TRANSLATIONS] as const) {
    test(`${name} has no empty string`, () => {
      const empty = leaves(catalogue).filter((v) => typeof v !== "string" || v.trim() === "");
      expect(empty).toEqual([]);
    });
  }
});
```

- [ ] **Paso 2: ejecútala y comprueba que falla**

```bash
bun test tests/unit/web-catalogue-parity.test.ts
```

Espera: `Cannot find module '../../web/src/i18n/catalogues/en'`.

- [ ] **Paso 3: instala las dependencias**

```bash
cd web && bun add i18next react-i18next i18next-resources-to-backend && cd ..
```

`i18next-resources-to-backend` son unas pocas líneas y es lo que convierte un
`import()` dinámico en el backend que i18next espera. Sin él habría que cargar
los doce ficheros por adelantado, que es justo lo que la carga perezosa evita.

- [ ] **Paso 4: escribe el namespace `common` en los tres idiomas**

`common` es lo que comparten todas las superficies: acciones, estados y errores
genéricos. Empieza pequeño; las tareas 5, 6 y 7 lo harán crecer.

`web/src/i18n/catalogues/en/common.ts`:

```ts
export default {
  language: {
    label: "Language",
  },
  action: {
    retry: "Try again",
    cancel: "Cancel",
    close: "Close",
    copy: "Copy",
    copied: "Copied",
  },
  state: {
    loading: "Loading…",
  },
  error: {
    unreachable: "Could not reach the server. Try again.",
    unexpected: "Something went wrong. Try again.",
  },
} as const;
```

`web/src/i18n/catalogues/es/common.ts`:

```ts
export default {
  language: {
    label: "Idioma",
  },
  action: {
    retry: "Reintentar",
    cancel: "Cancelar",
    close: "Cerrar",
    copy: "Copiar",
    copied: "Copiado",
  },
  state: {
    loading: "Cargando…",
  },
  error: {
    unreachable: "No se pudo contactar con el servidor. Inténtalo otra vez.",
    unexpected: "Algo ha fallado. Inténtalo otra vez.",
  },
} as const;
```

`web/src/i18n/catalogues/ca/common.ts`:

```ts
export default {
  language: {
    label: "Idioma",
  },
  action: {
    retry: "Torna-ho a provar",
    cancel: "Cancel·la",
    close: "Tanca",
    copy: "Copia",
    copied: "Copiat",
  },
  state: {
    loading: "Carregant…",
  },
  error: {
    unreachable: "No s'ha pogut contactar amb el servidor. Torna-ho a provar.",
    unexpected: "Alguna cosa ha fallat. Torna-ho a provar.",
  },
} as const;
```

- [ ] **Paso 5: escribe los tres índices, con los cuatro namespaces ya declarados**

Los tres ficheros `landing.ts`, `query.ts` y `chat.ts` de cada idioma se crean
**ahora, vacíos como objeto** (`export default {} as const;`), para que el índice
compile y la prueba de paridad los vigile desde el primer día. Son nueve ficheros
de una línea.

`web/src/i18n/catalogues/en/index.ts` — y el mismo contenido en `es/` y `ca/`:

```ts
import chat from "./chat";
import common from "./common";
import landing from "./landing";
import query from "./query";

export default { chat, common, landing, query };
```

- [ ] **Paso 6: escribe el arranque de i18next**

Crea `web/src/i18n/index.ts`. Este sí usa el alias `@/`, DOM y i18next: la suite
de la raíz no lo importa nunca.

```ts
import i18next from "i18next";
import { initReactI18next, useTranslation } from "react-i18next";
import resourcesToBackend from "i18next-resources-to-backend";
import { DEFAULT_LANGUAGE, isSupported, type Language } from "./languages";
import { readStoredLanguage, resolveLanguage, writeStoredLanguage } from "./resolve";
import { persistentStore } from "../lib/storage";
import type en from "./catalogues/en";

/**
 * What makes t("landing:hero.headline") a compile error when the key does not
 * exist. `en` is the source catalogue, so it is also the type.
 */
declare module "i18next" {
  interface CustomTypeOptions {
    defaultNS: "common";
    resources: typeof en;
  }
}

/**
 * One file per language and namespace, fetched on demand. Vite turns this
 * template into a glob over catalogues/*​/*.ts, so the landing never downloads
 * the chat's strings, nor the two languages it is not painting.
 */
const backend = resourcesToBackend(
  (language: string, namespace: string) => import(`./catalogues/${language}/${namespace}.ts`),
);

/** Called once, before the first render. */
export async function initI18n(): Promise<void> {
  const language = resolveLanguage({
    stored: readStoredLanguage(persistentStore()),
    navigator: navigator.languages,
  });

  await i18next
    .use(backend)
    .use(initReactI18next)
    .init({
      lng: language,
      fallbackLng: DEFAULT_LANGUAGE,
      ns: ["common"],
      defaultNS: "common",
      // React escapes for us; letting i18next do it too double-escapes every
      // apostrophe, and Catalan and Spanish are full of them.
      interpolation: { escapeValue: false },
    });

  applyDocumentLanguage(language);
}

/**
 * `persist` is the whole difference between the two callers. The selector
 * persists — it is a deliberate choice. /q/:token does not: writing the
 * agent's declaration to storage would make it beat every later declaration,
 * forever, from a decision this person never made.
 */
export async function changeLanguage(
  language: Language,
  { persist }: { persist: boolean },
): Promise<void> {
  if (persist) writeStoredLanguage(persistentStore(), language);
  await i18next.changeLanguage(language);
  applyDocumentLanguage(language);
}

function applyDocumentLanguage(language: Language): void {
  document.documentElement.lang = language;
}

/** The active language, for what is not text: dates, numbers, file sizes. */
export function useLanguage(): Language {
  const { i18n } = useTranslation();
  return isSupported(i18n.resolvedLanguage) ? i18n.resolvedLanguage : DEFAULT_LANGUAGE;
}
```

- [ ] **Paso 7: arranca i18next antes de pintar**

`web/src/main.tsx`, entero:

```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";
import { initI18n } from "./i18n";

// Rendering before the language is settled paints English and then swaps it,
// which is worse than waiting: the first thing somebody reads is the wrong
// thing. This is one dynamic import, not a network round trip.
void initI18n().then(() => {
  createRoot(document.getElementById("root")!).render(
    <StrictMode>
      <App />
    </StrictMode>,
  );
});
```

- [ ] **Paso 8: pon el límite de Suspense**

`useTranslation("landing")` suspende mientras carga ese namespace. Sin límite,
React lanza. En `web/src/App.tsx`, envuelve las rutas:

```tsx
import { Suspense } from "react";
import { Spinner } from "@/components/ui/Spinner";
```

```tsx
      <Suspense
        fallback={
          <div className="flex min-h-screen items-center justify-center">
            <Spinner />
          </div>
        }
      >
        <Routes>
          {/* … lo que ya había, sin tocar … */}
        </Routes>
      </Suspense>
```

- [ ] **Paso 9: quítale el idioma fijo al HTML**

`web/index.html:2`, que hoy dice `<html lang="en" class="dark">`:

```html
<!-- lang is stamped by the resolver at boot; en is what a crawler sees. -->
<html lang="en" class="dark">
```

No cambia el valor —es el que ve un rastreador y el que vale hasta que arranca
JavaScript— pero el comentario evita que alguien lo "arregle" creyendo que
manda. El `<title>` y las etiquetas Open Graph siguen en inglés: son para
rastreadores y para quien comparte el enlace, y las URLs por idioma están fuera
de alcance por decisión del spec.

- [ ] **Paso 10: comprueba que todo pasa**

```bash
bun test tests/unit/web-catalogue-parity.test.ts tests/unit/web-language-resolution.test.ts
cd web && bunx tsc -b && cd ..
```

Y a mano, que es lo único que prueba el arranque de verdad:

```bash
cd web && bun run dev
```

Abre `http://localhost:5173`, y en la consola del navegador:

```js
localStorage.setItem("agentdialog:language", "ca"); location.reload();
document.documentElement.lang   // "ca"
```

Espera: `ca`. La página sigue en inglés —no hay ni una cadena traducida
todavía— y eso es correcto en esta tarea. Deja el `localStorage` limpio después:
`localStorage.removeItem("agentdialog:language")`.

- [ ] **Paso 11: commit**

```bash
git add web/package.json web/bun.lock web/src/i18n web/src/main.tsx web/src/App.tsx \
  web/index.html tests/unit/web-catalogue-parity.test.ts
git commit -m "Wire i18next, with one file per language and namespace"
```

---

## Task 3: El selector

**Files:**
- Create: `web/src/components/i18n/LanguageSelector.tsx`
- Modify: `web/src/components/layout/Footer.tsx`
- Modify: `web/src/components/layout/Sidebar.tsx`

**Interfaces:**
- Consumes: `changeLanguage`, `useLanguage` de `@/i18n`; `SUPPORTED_LANGUAGES`,
  `LANGUAGE_NAMES`, `isSupported` de `@/i18n/languages`; `common:language.label`
- Produces: `<LanguageSelector className?: string />`

- [ ] **Paso 1: escribe el componente**

Crea `web/src/components/i18n/LanguageSelector.tsx`:

```tsx
import { useTranslation } from "react-i18next";
import { changeLanguage, useLanguage } from "@/i18n";
import { LANGUAGE_NAMES, SUPPORTED_LANGUAGES, isSupported } from "@/i18n/languages";
import { cn } from "@/lib/cn";

/**
 * The same control on the landing page, on the answer page and in the chat.
 *
 * It matters most on /q/:token, which is the one surface where the language was
 * chosen by somebody else — the agent declared it, and agents will get it
 * wrong. This is how that person fixes it without an account.
 *
 * A native <select> on purpose: it is keyboard- and screen-reader-correct for
 * free, and it is what a phone renders as a proper picker.
 */
export function LanguageSelector({ className }: { className?: string }) {
  const { t } = useTranslation("common");
  const language = useLanguage();

  return (
    <label className={cn("inline-flex items-center gap-2", className)}>
      <span className="sr-only">{t("language.label")}</span>
      <select
        value={language}
        onChange={(event) => {
          const next = event.target.value;
          // The choice is deliberate, so it persists — and from here on it
          // outranks anything an agent declares.
          if (isSupported(next)) void changeLanguage(next, { persist: true });
        }}
        className="rounded border border-surface-border bg-surface-secondary px-2 py-1 text-sm text-gray-300 hover:text-gray-100 focus:border-brand-600 focus:outline-none"
      >
        {SUPPORTED_LANGUAGES.map((option) => (
          <option key={option} value={option}>
            {LANGUAGE_NAMES[option]}
          </option>
        ))}
      </select>
    </label>
  );
}
```

- [ ] **Paso 2: móntalo en el pie**

`web/src/components/layout/Footer.tsx` lo pintan las dos rutas públicas —la
landing con `PublicLayout` y `/q/:token` con `BareLayout minimal`—, así que un
solo montaje cubre las dos superficies.

Importa el componente y ponlo el primero del grupo derecho, **fuera** del
`{!minimal && ...}`:

```tsx
          <div className="flex items-center gap-6">
            <LanguageSelector />
            <p className="text-sm text-gray-400">
              Agent-first messaging platform. Built for the AI era.
            </p>
            {!minimal && (
```

Que quede fuera de `minimal` es deliberado, y merece un comentario en el
componente `Footer`, encima del selector:

```tsx
            {/* Kept in minimal mode. That flag drops links which invite
                somebody away from the one decision the page exists for; a
                language picker does the opposite — it is what lets them read
                the decision at all. */}
```

- [ ] **Paso 3: móntalo en la barra lateral del chat**

`web/src/components/layout/Sidebar.tsx`, en el bloque inferior con borde
superior, **encima** de la fila del usuario:

```tsx
        <div className="border-t border-surface-border p-3">
          <div className="mb-3">
            <LanguageSelector className="w-full [&>select]:w-full" />
          </div>
          <div className="flex items-center gap-3">
            <Avatar name={human?.displayName ?? human?.email ?? "User"} size="sm" />
```

- [ ] **Paso 4: compruébalo a mano**

```bash
cd web && bunx tsc -b && bun run dev
```

Tres comprobaciones, y las tres importan:

1. En `http://localhost:5173`, el selector aparece en el pie. Elige **Català**:
   `document.documentElement.lang` pasa a `ca`. La página sigue en inglés — no
   hay cadenas traducidas todavía.
2. Recarga: el selector sigue en Català. Eso es el `localStorage`.
3. `localStorage.getItem("agentdialog:language")` devuelve `"ca"`.

Déjalo en English antes de seguir.

- [ ] **Paso 5: commit**

```bash
git add web/src/components/i18n/LanguageSelector.tsx \
  web/src/components/layout/Footer.tsx web/src/components/layout/Sidebar.tsx
git commit -m "One language picker, on all three surfaces"
```

---

## Task 4: Las fechas, las horas y los tamaños

`web/src/lib/formatters.ts` es hoy la segunda fuente de idioma de la aplicación
y nadie la llamó así: tiene cadenas en inglés a mano y un `toLocaleDateString()`
sin locale, que se lleva el del navegador. Si esto no se arregla, alguien que
elija catalán en un navegador en inglés verá `3d ago` dentro de una página en
catalán.

**Files:**
- Modify: `web/src/lib/formatters.ts`
- Modify: los seis sitios que las llaman (ver el paso 3)

**Interfaces:**
- Consumes: `localeTag`, `type Language` de `@/i18n/languages`
- Produces: `formatRelativeTime(date, language)`, `formatTime(date, language)`,
  `formatDuration(ms, language)`, `formatFileSize(bytes, language)` — **las cuatro
  con un segundo argumento obligatorio**, que es lo que obliga al compilador a
  señalar cada llamada que aún no lo pasa

- [ ] **Paso 1: reescribe los formateadores**

`web/src/lib/formatters.ts`, entero:

```ts
import { localeTag, type Language } from "@/i18n/languages";

/**
 * These are not components and cannot read a hook, so the language arrives as
 * an argument. It is required rather than defaulted on purpose: a default is a
 * silent way to keep rendering English inside a Catalan page.
 */

const MINUTE = 60;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;
const WEEK = 7 * DAY;

export function formatRelativeTime(date: string | Date, language: Language): string {
  const seconds = Math.round((new Date(date).getTime() - Date.now()) / 1000);
  const elapsed = Math.abs(seconds);
  // `numeric: "auto"` is what turns -1 day into "yesterday" / "ayer" / "ahir"
  // instead of "1 day ago", in every language, without a plural rule of ours.
  const relative = new Intl.RelativeTimeFormat(localeTag(language), { numeric: "auto" });

  if (elapsed < MINUTE) return relative.format(0, "second");
  if (elapsed < HOUR) return relative.format(Math.round(seconds / MINUTE), "minute");
  if (elapsed < DAY) return relative.format(Math.round(seconds / HOUR), "hour");
  if (elapsed < WEEK) return relative.format(Math.round(seconds / DAY), "day");

  return new Date(date).toLocaleDateString(localeTag(language));
}

export function formatTime(date: string | Date, language: Language): string {
  return new Date(date).toLocaleTimeString(localeTag(language), {
    hour: "2-digit",
    minute: "2-digit",
  });
}

/**
 * Units, not words: ms, s and m read the same in the three languages. What does
 * change is the decimal separator — 1.5s is 1,5s in Spanish and Catalan.
 */
export function formatDuration(ms: number, language: Language): string {
  const number = new Intl.NumberFormat(localeTag(language), { maximumFractionDigits: 1 });

  if (ms < 1000) return `${number.format(ms)}ms`;
  const seconds = ms / 1000;
  if (seconds < 60) return `${number.format(seconds)}s`;

  const minutes = Math.floor(seconds / 60);
  const remaining = Math.floor(seconds % 60);
  return `${number.format(minutes)}m ${number.format(remaining)}s`;
}

export function formatFileSize(bytes: number, language: Language): string {
  const number = new Intl.NumberFormat(localeTag(language), { maximumFractionDigits: 1 });

  if (bytes < 1024) return `${number.format(bytes)} B`;
  if (bytes < 1024 * 1024) return `${number.format(bytes / 1024)} KB`;
  if (bytes < 1024 * 1024 * 1024) return `${number.format(bytes / (1024 * 1024))} MB`;
  return `${number.format(bytes / (1024 * 1024 * 1024))} GB`;
}
```

- [ ] **Paso 2: deja que el compilador te enseñe las llamadas**

```bash
cd web && bunx tsc -b
```

Espera: seis errores, `Expected 2 arguments, but got 1`, en

- `src/components/chat/messages/FileMessage.tsx:86` y `:104`
- `src/components/invitations/InvitationCard.tsx:57`
- `src/components/chat/messages/ToolResultMessage.tsx:20`
- `src/components/chat/messages/MessageBubble.tsx:39`
- `src/components/chat/ConversationItem.tsx:32`
- `src/components/layout/Sidebar.tsx:111`

(Son siete llamadas en seis ficheros.)

- [ ] **Paso 3: pasa el idioma en cada una**

En cada uno de esos seis componentes, añade el import y la línea del hook, y pasa
`language` como segundo argumento. El patrón, idéntico en los seis:

```tsx
import { useLanguage } from "@/i18n";
```

```tsx
  const language = useLanguage();
```

```tsx
  {formatRelativeTime(conversation.updatedAt, language)}
```

Cuidado con dos:

- `Sidebar.tsx` ya tiene cuerpo de función y otros hooks: `useLanguage()` va con
  ellos, arriba, nunca dentro del `.map()`.
- `MessageBubble.tsx` y `ToolResultMessage.tsx` son componentes pequeños; si
  alguno resulta no ser un componente sino una función auxiliar, pásale
  `language` como prop desde quien lo pinta en vez de llamar al hook.

- [ ] **Paso 4: comprueba que compila y míralo**

```bash
cd web && bunx tsc -b && bun run dev
```

Entra en el chat con una sesión, elige **Español** en el selector de la barra
lateral y mira las marcas de tiempo de las conversaciones: `hace 3 días`, no
`3d ago`. En catalán, `fa 3 dies`.

- [ ] **Paso 5: commit**

```bash
git add web/src/lib/formatters.ts \
  web/src/components/chat/messages/FileMessage.tsx \
  web/src/components/chat/messages/ToolResultMessage.tsx \
  web/src/components/chat/messages/MessageBubble.tsx \
  web/src/components/chat/ConversationItem.tsx \
  web/src/components/invitations/InvitationCard.tsx \
  web/src/components/layout/Sidebar.tsx
git commit -m "Stop formatters from being a second, hidden source of language"
```

---

## Task 5: La landing

La superficie más grande y la que motivó todo esto. Al final, agentdialog.io se
pinta entera en los tres idiomas.

**Files:**
- Modify: `web/src/i18n/catalogues/{en,es,ca}/landing.ts`
- Modify: los once de `web/src/components/landing/`, `web/src/pages/LandingPage.tsx`,
  `web/src/components/layout/{Navbar,Footer}.tsx`, `web/src/pages/NotFoundPage.tsx`
- Modify: `web/eslint.config.js`
- Modify: `web/package.json` (`eslint-plugin-i18next`)

**Interfaces:**
- Consumes: `useTranslation`, `Trans` de `react-i18next`
- Produces: el namespace `landing` con estas secciones, y ninguna otra:
  `nav`, `hero`, `flow`, `features`, `how`, `examples`, `guide`, `faq`, `cta`,
  `form`, `footer`, `notFound`

**Convención de claves**, y se respeta en las tres tareas de traducción:
`namespace:seccion.clave`, dos niveles, en `camelCase`. La sección se llama como
el componente en minúscula (`Hero` → `hero`, `HowItWorks` → `how`). Ejemplos:
`landing:hero.headline`, `landing:faq.items.inboundEmail.answer`.

- [ ] **Paso 1: instala la red de seguridad y hazla fallar**

Con ~200 cadenas en once ficheros, «¿las he sacado todas?» no puede ser una
pregunta que conteste alguien mirando. Se contesta con el linter.

```bash
cd web && bun add -d eslint-plugin-i18next && cd ..
```

En `web/eslint.config.js`, añade el import y **un bloque nuevo al final del
array**, acotado a la landing:

```js
import i18next from 'eslint-plugin-i18next'
```

```js
  {
    // Turned on directory by directory as each surface is translated. A literal
    // left in the JSX is the one failure mode a key-parity test cannot see:
    // the catalogues agree perfectly and the string was never in them.
    files: [
      'src/components/landing/**/*.tsx',
      'src/pages/LandingPage.tsx',
      'src/pages/NotFoundPage.tsx',
      'src/components/layout/Navbar.tsx',
      'src/components/layout/Footer.tsx',
    ],
    plugins: { i18next },
    rules: {
      // jsx-text-only: only text nodes. Checking attributes as well drowns the
      // signal in className and viewBox.
      'i18next/no-literal-string': ['error', { mode: 'jsx-text-only' }],
    },
  },
```

```bash
cd web && bunx eslint .
```

Espera: decenas de errores `disallow literal string`, uno por cadena sin
traducir. **Esa lista es la tarea.**

Si el plugin no arrancara con la configuración plana de ESLint 9, no pelees con
él más de diez minutos: quítalo, anota en el commit que la comprobación se hizo
a mano, y recorre los once ficheros con
`grep -nE '>[A-Za-z]' src/components/landing/*.tsx`. La red de seguridad es
deseable, no es el objetivo.

- [ ] **Paso 2: traduce el ejemplo completo — `Hero.tsx`**

Este es el patrón que siguen los otros diez. Tiene los tres casos difíciles:
texto con marcado dentro, una constante a nivel de módulo, y un icono que no se
traduce.

En `web/src/i18n/catalogues/en/landing.ts`:

```ts
export default {
  hero: {
    badge: "The agent-first messaging platform",
    // <accent> is the brand-coloured span. Translators move it with the words:
    // in Spanish the emphasis lands on a different part of the sentence.
    headline: "Your agents ask. <accent>Your team answers</accent> in one click.",
    subhead:
      "When your AI agent needs a human decision, it sends one API call. Your team gets an email, signs in with the code it carries, and answers in the chat. No account to create. No password. No context lost.",
    docsLink: "Or read the docs first",
    reassurance: {
      noCard: {
        title: "No credit card",
        detail: "The form asks for one thing: your agent's name.",
      },
      noAccount: {
        title: "No account to create",
        detail: "Your team answers in the chat the email points to.",
      },
      fast: {
        title: "Key in 15 seconds",
        detail: "Shown once, on this page. Copy it and you're integrating.",
      },
    },
  },
} as const;
```

En `es/landing.ts`:

```ts
export default {
  hero: {
    badge: "La plataforma de mensajería pensada para agentes",
    headline: "Tus agentes preguntan. <accent>Tu equipo responde</accent> en un clic.",
    subhead:
      "Cuando tu agente de IA necesita que decida una persona, envía una llamada a la API. Tu equipo recibe un correo, entra con el código que lleva dentro y responde en el chat. Sin crear cuenta. Sin contraseña. Sin perder el contexto.",
    docsLink: "O lee primero la documentación",
    reassurance: {
      noCard: {
        title: "Sin tarjeta de crédito",
        detail: "El formulario pide una cosa: el nombre de tu agente.",
      },
      noAccount: {
        title: "Sin cuenta que crear",
        detail: "Tu equipo responde en el chat al que apunta el correo.",
      },
      fast: {
        title: "Tu clave en 15 segundos",
        detail: "Se muestra una sola vez, en esta página. Cópiala y a integrar.",
      },
    },
  },
} as const;
```

En `ca/landing.ts`:

```ts
export default {
  hero: {
    badge: "La plataforma de missatgeria pensada per a agents",
    headline: "Els teus agents pregunten. <accent>El teu equip respon</accent> en un clic.",
    subhead:
      "Quan el teu agent d'IA necessita que decideixi una persona, envia una crida a l'API. El teu equip rep un correu, hi entra amb el codi que porta dins i respon al xat. Sense crear cap compte. Sense contrasenya. Sense perdre el context.",
    docsLink: "O llegeix primer la documentació",
    reassurance: {
      noCard: {
        title: "Sense targeta de crèdit",
        detail: "El formulari demana una cosa: el nom del teu agent.",
      },
      noAccount: {
        title: "Sense cap compte a crear",
        detail: "El teu equip respon al xat on apunta el correu.",
      },
      fast: {
        title: "La teva clau en 15 segons",
        detail: "Es mostra una sola vegada, en aquesta pàgina. Copia-la i ja pots integrar.",
      },
    },
  },
} as const;
```

Y `web/src/components/landing/Hero.tsx`. Fíjate en las tres cosas: `REASSURANCES`
conserva los iconos y pierde el texto, quedándose con un `id` que es la clave;
el `<h1>` usa `<Trans>` porque lleva un `<span>` dentro; y el `useTranslation`
declara el namespace.

```tsx
import { useEffect } from "react";
import { Trans, useTranslation } from "react-i18next";
import { GetKeyForm } from "@/components/landing/GetKeyForm";
import { rememberAttribution } from "@/lib/attribution";
import { sessionStore } from "@/lib/storage";

/**
 * The icons stay here; their words moved to the catalogue. Each entry keeps the
 * id that names its keys, so a translator never meets an SVG.
 */
const REASSURANCES = [
  { id: "noCard", icon: (/* … el mismo SVG de la tarjeta, sin tocar … */) },
  { id: "noAccount", icon: (/* … */) },
  { id: "fast", icon: (/* … */) },
] as const;

export function Hero() {
  const { t } = useTranslation("landing");

  useEffect(() => {
    rememberAttribution(window.location.search, sessionStore());
  }, []);

  return (
    <section id="get-key" className="relative overflow-hidden bg-surface-primary">
      <div className="mx-auto max-w-7xl px-4 pb-12 pt-24 sm:px-6 sm:pb-16 sm:pt-32 lg:px-8">
        <div className="mx-auto max-w-2xl text-center">
          <div className="mb-8 inline-flex items-center gap-2 rounded-full border border-brand-800 bg-brand-950 px-4 py-1.5 text-sm text-brand-300">
            <span className="flex h-2 w-2 rounded-full bg-brand-500" />
            {t("hero.badge")}
          </div>

          <h1 className="text-4xl font-bold tracking-tight text-gray-100 sm:text-6xl">
            <Trans
              t={t}
              i18nKey="hero.headline"
              components={{ accent: <span className="text-brand-600" /> }}
            />
          </h1>

          <p className="mt-6 text-lg leading-8 text-gray-400">{t("hero.subhead")}</p>

          <div className="mt-10 flex flex-col items-center gap-5">
            <GetKeyForm />
          </div>
        </div>

        <ul className="mx-auto mt-12 grid max-w-5xl gap-4 sm:grid-cols-3">
          {REASSURANCES.map((item) => (
            <li
              key={item.id}
              className="rounded-xl border border-surface-border bg-surface-secondary p-5 text-left"
            >
              <span className="inline-flex text-brand-400" aria-hidden="true">
                {item.icon}
              </span>
              <p className="mt-3.5 text-base font-semibold text-gray-100">
                {t(`hero.reassurance.${item.id}.title` as const)}
              </p>
              <p className="mt-1 text-sm leading-snug text-gray-400">
                {t(`hero.reassurance.${item.id}.detail` as const)}
              </p>
            </li>
          ))}
        </ul>

        <div className="mt-8 text-center">
          <a
            href="https://docs.agentdialog.io"
            target="_blank"
            rel="noopener noreferrer"
            className="text-sm text-gray-400 underline underline-offset-4 hover:text-gray-200"
          >
            {t("hero.docsLink")}
          </a>
        </div>
      </div>

      <div className="absolute inset-x-0 -top-40 -z-10 transform-gpu overflow-hidden blur-3xl sm:-top-80">
        <div className="relative left-[calc(50%-11rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 rotate-[30deg] bg-gradient-to-tr from-brand-700 to-brand-500 opacity-10 sm:left-[calc(50%-30rem)] sm:w-[72.1875rem]" />
        </div>
    </section>
  );
}
```

Si la clave interpolada de `t(\`hero.reassurance.${item.id}.title\`)` no
convence al tipado, declara el mapa explícito y sáltate la plantilla:

```tsx
const KEYS = {
  noCard: { title: "hero.reassurance.noCard.title", detail: "hero.reassurance.noCard.detail" },
  noAccount: { title: "hero.reassurance.noAccount.title", detail: "hero.reassurance.noAccount.detail" },
  fast: { title: "hero.reassurance.fast.title", detail: "hero.reassurance.fast.detail" },
} as const;
```

- [ ] **Paso 3: recorre los diez ficheros restantes**

Mismo patrón, uno a uno, y **commit cada dos o tres** para que la revisión sea
legible. Lo que lleva cada uno:

| Fichero | Sección | Qué NO se traduce |
|---|---|---|
| `Navbar.tsx` | `nav` | «AgentDialog» |
| `FlowDemo.tsx` | `flow` | Nada de texto, pero **ojo**: son 787 líneas con etiquetas dentro de una animación con tiempos; traduce solo las cadenas y no toques ni un `setTimeout` ni una clase |
| `Features.tsx` | `features` | Los nombres de producto |
| `HowItWorks.tsx` | `how` | — |
| `CodeExamples.tsx` | `examples` | **El código: ni el `curl`, ni el TypeScript, ni los nombres de campo.** Solo títulos, pestañas y prosa |
| `IntegrationGuide.tsx` | `guide` | El markdown que pinta, que es un fichero generado. Solo el marco: título, botón de abrir/cerrar |
| `Faq.tsx` | `faq` | — (nueve preguntas con su respuesta: es el fichero con más palabras después de `FlowDemo`) |
| `CTA.tsx` | `cta` | — |
| `GetKeyForm.tsx` | `form` | El prefijo `mge_ag_`, y los mensajes de error que vienen de la API |
| `Footer.tsx` | `footer` | «AgentDialog», «GitHub», «Docs» |
| `LandingPage.tsx`, `NotFoundPage.tsx` | `notFound` | — |

Reglas para las traducciones, que valen también para las tareas 6 y 7:

- **Tutea.** «Tus agentes preguntan», no «Sus agentes preguntan».
- **No calques la sintaxis inglesa.** Si una frase en español queda rara, reescríbela: el catálogo no es una traducción palabra a palabra, es la misma promesa dicha bien.
- **Los términos del producto no se traducen**: AgentDialog, MCP, API, API key, webhook, SDK, token, endpoint.
- **Nada de género marcado innecesario.** «Quien responde», no «el usuario responde».
- El catalán lo revisará alguien que lo hable; escríbelo lo mejor que puedas y no lo bloquees por eso.

- [ ] **Paso 4: el linter tiene que quedarse callado**

```bash
cd web && bunx eslint . && bunx tsc -b
```

Espera: cero errores de `i18next/no-literal-string`, cero de tipos.

- [ ] **Paso 5: la paridad tiene que seguir verde**

```bash
bun test tests/unit/web-catalogue-parity.test.ts
```

Espera: verde. Si falla, dice exactamente qué clave falta en `es` o en `ca` —
esa es toda la información que necesitas.

- [ ] **Paso 6: míralo con los tres idiomas**

```bash
cd web && bun run dev
```

Recorre la landing entera en inglés, español y catalán. Mira específicamente:
el `<h1>`, que el `<accent>` haya caído donde toca en cada idioma; la FAQ, que
los desplegables no se hayan roto; y `FlowDemo`, que la animación siga corriendo
una sola vez y quedándose en su estado final.

- [ ] **Paso 7: commit**

```bash
git add web/src/i18n/catalogues web/src/components/landing web/src/components/layout \
  web/src/pages/LandingPage.tsx web/src/pages/NotFoundPage.tsx \
  web/eslint.config.js web/package.json web/bun.lock
git commit -m "Put the landing page in three languages"
```

---

## Task 6: `/q/:token` y los componentes de query

Aquí entra la única pieza de precedencia que no existe en ninguna otra
superficie: el idioma declarado por el agente.

**Files:**
- Modify: `web/src/i18n/catalogues/{en,es,ca}/query.ts`
- Modify: `web/src/pages/PublicQueryPage.tsx`
- Modify: `web/src/components/queries/{QueryContextHeader,QueryCard,InsufficientContextControl}.tsx`
- Modify: `web/src/components/answer/*.tsx` (los siete)
- Modify: `web/eslint.config.js` (añadir estos directorios a la regla)

**Interfaces:**
- Consumes: `resolveLanguage` de `@/i18n/resolve`, `changeLanguage` de `@/i18n`,
  `readStoredLanguage` y `persistentStore`
- Produces: el namespace `query`, secciones `page`, `context`, `answer`, `reasons`, `card`

- [ ] **Paso 1: lee el idioma que ya viene en la respuesta**

`shapeHumanQuery` devuelve `language` desde la v0.8.9, así que
`GET /public/queries/:token` ya lo trae. La página no lo declara — el mismo
descuido que tuvo con `changes`.

En `web/src/pages/PublicQueryPage.tsx`, en la interfaz `PublicQuery`:

```ts
  answer_space: AnswerSpace;
  // Declared by the agent when it asked. It is a hint about who is reading,
  // not a translation of the question: the words below are the agent's own.
  language?: string | null;
```

Y en el `useEffect` que carga la query, justo después de `setState`:

```ts
        const body = await res.json();
        setState({ status: "ready", query: body.data });

        // Precedence, and the one place all three sources meet. `persist:
        // false` is load-bearing: writing the agent's declaration to storage
        // would make it outrank every later declaration for good, from a
        // choice this person never made.
        void changeLanguage(
          resolveLanguage({
            stored: readStoredLanguage(persistentStore()),
            declared: body.data.language,
            navigator: navigator.languages,
          }),
          { persist: false },
        );
```

con los imports:

```ts
import { changeLanguage } from "@/i18n";
import { persistentStore } from "@/lib/storage";
import { readStoredLanguage, resolveLanguage } from "@/i18n/resolve";
```

- [ ] **Paso 2: traduce la página**

Las cadenas de `PublicQueryPage.tsx`, con sus claves. Escribe las tres versiones
en `catalogues/{en,es,ca}/query.ts` bajo `page`, siguiendo el patrón de la tarea 5:

| Clave | Inglés |
|---|---|
| `page.loading` | `Loading…` (o reutiliza `common:state.loading`) |
| `page.gone.title` | `This link no longer works` |
| `page.gone.body` | `It may have been used already, or the question may have closed. Sign in to the app to see anything still waiting for you.` |
| `page.answered.title` | `Answer sent` |
| `page.answered.body` | `Thank you — you can close this page.` |
| `page.returned.title` | `Sent back for more detail` |
| `page.returned.body` | `They will get back to you. This link keeps working, so you can return to it.` |
| `page.asking` | `is asking you a question` |
| `page.contextLabel` | `Context` |
| `page.send` | `Send answer` |
| `page.cantAnswer` | `I can't answer this` |
| `page.sendBack` | `Send it back` |
| `page.sendFailed` | `That didn't send. Try again.` |

`Could not reach the server. Try again.` ya está en `common:error.unreachable` de
la tarea 2: úsala, no la dupliques.

Y las cinco razones del `const REASONS`, que pierden su `label` y se quedan con
el `id`, igual que hizo `REASSURANCES` en la tarea 5:

| Clave | Inglés |
|---|---|
| `reasons.unknown_subject` | `I don't know what this is about` |
| `reasons.missing_delta` | `I don't know what changed since last time` |
| `reasons.unclear_consequences` | `I can't tell what each option would do` |
| `reasons.referent_unreachable` | `I can't see the thing being asked about` |
| `reasons.not_my_decision` | `This isn't mine to decide` |

- [ ] **Paso 3: traduce los componentes compartidos**

`QueryContextHeader.tsx` es el que pintan **las dos** superficies, la pública y
el chat. Bajo `context`:

| Clave | Inglés |
|---|---|
| `context.about` | `About` |
| `context.priorDecision` | `You decided about this on {{date}}.` |
| `context.selfContained` | `Self-contained — nothing else to look at.` |
| `context.openLink` | `Open referenced link` |
| `context.showReferent` | `Show referent` |
| `context.hideReferent` | `Hide referent` |
| `context.whatChanged` | `What changed` |
| `context.material` | `material` |

Ojo con `priorDecision`: hoy hace `new Date(priorDecisionAt).toLocaleDateString()`
sin locale. Pásalo por `useLanguage()` y `localeTag`, como en la tarea 4:

```tsx
        <p className="text-xs text-amber-400">
          {t("context.priorDecision", {
            date: new Date(priorDecisionAt).toLocaleDateString(localeTag(language)),
          })}
        </p>
```

Y la flecha `&#8599;` de `Open referenced link` se queda **fuera** del texto
traducido, en su propio `<span aria-hidden="true">`: es un icono, no una palabra.

Después, los siete de `web/src/components/answer/` (`AnswerSpaceInput`,
`BooleanAnswer`, `ChoiceAnswer`, `DateAnswer`, `FieldsAnswer`, `ScalarAnswer`,
`TextAnswer`) y `InsufficientContextControl.tsx`, bajo `answer`. Aquí la regla de
qué no se traduce es la más importante de todo el plan: **las etiquetas de las
opciones, los nombres de los campos y las unidades vienen del `answer_space` que
escribió el agente y se pintan tal cual.** Lo que se traduce es el marco: «Yes» /
«No» cuando los pone el componente, los placeholders, los textos de validación,
los botones.

Por último `QueryCard.tsx`, bajo `card`.

- [ ] **Paso 4: extiende el linter a estos directorios**

En `web/eslint.config.js`, añade al array `files` del bloque de la tarea 5:

```js
      'src/pages/PublicQueryPage.tsx',
      'src/components/queries/**/*.tsx',
      'src/components/answer/**/*.tsx',
```

```bash
cd web && bunx eslint . && bunx tsc -b
```

Espera: cero errores. Si aparece alguno, es una cadena que se te ha quedado en
el JSX.

- [ ] **Paso 5: pruébalo de punta a punta, que es donde esto se cae**

Esta es la única forma de ver la precedencia funcionando, y el camino está
documentado porque cuesta encontrarlo:

```bash
docker compose -f docker-compose.dev.yml up -d postgres redis minio mailhog
# .env: APP_URL=http://localhost:5173 — el 3000 es la API y el enlace acaba en un 404
bun run dev          # en la raíz
cd web && bun run dev
```

Crea una query de riesgo `low` o `medium` —solo esas acuñan enlace, lo decide
`shouldMintGrant`— con `language: "ca"`, saca el enlace `/q/...` del correo en
`http://localhost:8025`, y comprueba las tres:

1. **Sin nada guardado**: la página sale **en catalán**, aunque tu navegador esté
   en inglés. Eso es «declarado gana a navegador».
2. **Elige Español en el selector** y recarga el mismo enlace: sale **en
   español**. Eso es «selector gana a declarado».
3. `localStorage.removeItem("agentdialog:language")` y recarga: vuelve al
   catalán. Eso confirma que el declarado **no** se guardó.

La tercera es la que importa: si sale español, `persist: false` no está donde
debe.

- [ ] **Paso 6: commit**

```bash
git add web/src/i18n/catalogues web/src/pages/PublicQueryPage.tsx \
  web/src/components/queries web/src/components/answer web/eslint.config.js
git commit -m "Answer page in the language the agent declared, unless you said otherwise"
```

---

## Task 7: El chat y el resto de la aplicación

**Files:**
- Modify: `web/src/i18n/catalogues/{en,es,ca}/chat.ts` y `common.ts`
- Modify: `web/src/components/chat/**` (21 ficheros), `web/src/components/auth/*`,
  `web/src/components/invitations/*`, `web/src/components/profile/*`,
  `web/src/components/ui/*`, `web/src/components/layout/{Sidebar,Navbar,DashboardLayout}.tsx`,
  `web/src/components/ErrorBoundary.tsx`, y `web/src/pages/{DashboardPage,LoginPage,InvitationsPage,SettingsPage,QueriesPage,TrustedAgentsPage}.tsx`
- Modify: `web/eslint.config.js`

**Interfaces:**
- Consumes: todo lo anterior
- Produces: el namespace `chat` (secciones `list`, `header`, `input`, `messages`,
  `empty`, `queries`, `invitations`, `settings`, `agents`, `auth`) y lo que caiga
  en `common` — botones, estados y errores que aparecen en más de una superficie

- [ ] **Paso 1: extiende el linter al resto y mira la lista**

En `web/eslint.config.js`, sustituye el array `files` acotado por el general, que
a partir de aquí es todo:

```js
    files: ['src/**/*.tsx'],
```

```bash
cd web && bunx eslint .
```

Espera: la lista de todo lo que queda. **Es la tarea.** Anótala antes de empezar
—`bunx eslint . -f compact > /tmp/pending.txt`— porque irá encogiendo y es la
única barra de progreso que hay.

- [ ] **Paso 2: traduce por carpetas, y commitea por carpeta**

Orden sugerido, de menos a más enredado, y un commit por línea:

1. `components/ui/*` — Alert, Button, CodeBlock, Modal, Spinner: cuatro o cinco
   cadenas, todas a `common`.
2. `components/auth/*` y `pages/LoginPage.tsx` — el formulario de acceso, sus
   errores y el texto del código por correo.
3. `components/invitations/*` y `pages/InvitationsPage.tsx`.
4. `components/profile/*` y `pages/SettingsPage.tsx`.
5. `pages/TrustedAgentsPage.tsx` y `pages/QueriesPage.tsx`.
6. `components/chat/**` — el grueso: la lista, la cabecera, el compositor, el
   indicador de escritura, los estados vacíos y los trece renderizadores de
   mensaje de `chat/messages/`.
7. `components/layout/*`, `ErrorBoundary.tsx` y `pages/DashboardPage.tsx`.

Las mismas reglas de la tarea 5 —tuteo, nada de calcos, términos de producto sin
traducir— y una más, que aquí es la que se rompe: **lo que escribió el agente o
lo que escribió una persona no se traduce nunca.** En `chat/messages/` casi todo
el texto en pantalla es contenido, no interfaz. Lo que se traduce es la etiqueta
alrededor: «Tool call», «Approved», «Sent», «Downloading…», las horas, los
estados.

- [ ] **Paso 3: el linter callado y los tipos verdes**

```bash
cd web && bunx eslint . && bunx tsc -b
```

Si alguna cadena legítimamente no es traducible y el linter insiste —un nombre
propio, una unidad—, silénciala **en esa línea** con
`{/* eslint-disable-next-line i18next/no-literal-string */}` y un motivo. Nunca
en el fichero entero, y nunca en la configuración.

- [ ] **Paso 4: la paridad, y la suite entera**

```bash
bun test tests/unit tests/integration
```

Espera: verde. Estas tareas no tocan el backend, así que una integración roja
aquí es o Redis con el contador de altas lleno —`redis-cli -n 1 FLUSHDB` contra
la base de datos de pruebas— o algo que no es tuyo.

- [ ] **Paso 5: míralo con sesión**

```bash
cd web && bun run dev
```

Entra en `/app`, cambia el idioma en la barra lateral y recorre: la lista de
conversaciones, un hilo con mensajes de varios tipos, la página de queries, las
invitaciones y los ajustes. Comprueba en particular que **el contenido de los
mensajes sigue exactamente como se escribió** — si algo de lo que dijo un agente
o una persona ha cambiado de idioma, eso es un fallo, no una traducción.

- [ ] **Paso 6: commit**

Ya has ido commiteando por carpeta. Cierra con lo que quede suelto:

```bash
git add web/src
git commit -m "Put the signed-in app in three languages"
```

---

## Task 8: CI, roadmap y la convención del repo

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `docs-site/content/docs/roadmap.mdx`
- Modify: `CLAUDE.md`

- [ ] **Paso 1: dale a `web/` un job propio**

Hoy CI corre la suite del backend, `tsc --noEmit` en la raíz y `biome check src/`.
No mira `web/` en absoluto: un error de tipos ahí lo descubre Cloudflare Pages
**después** de fusionar, porque Pages publica la landing al entrar en `main` sin
esperar a una release. Este trabajo toca del orden de cuarenta ficheros de `web/`,
así que le toca cerrarlo.

Añade al final de `.github/workflows/ci.yml`, al mismo nivel que el job del SDK:

```yaml
  web:
    name: Web typecheck and lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install
        working-directory: web
        run: bun install --frozen-lockfile

      - name: Typecheck
        working-directory: web
        run: bunx tsc -b

      # The literal-string rule is what keeps a new hard-coded string out of the
      # JSX. Without it, the catalogues stay perfectly in sync and the page
      # quietly goes back to being English.
      - name: Lint
        working-directory: web
        run: bunx eslint .
```

Copia el `uses:` y la versión exactos de los jobs que ya están en ese fichero, no
los de aquí, si no coinciden.

Las dos pruebas de i18n **no** necesitan nada: viven en `tests/unit/` y el job
que ya existe las ejecuta. Ese fue el motivo de ponerlas ahí.

- [ ] **Paso 2: cuéntalo en el roadmap**

En `docs-site/content/docs/roadmap.mdx`, bajo `## Available now`, la entrada
**Emails in the language you declare** deja de hablar solo de correo. Sustituye
su primer párrafo por:

```mdx
**Three languages, on the page and in the mail.** The landing page, the answer
page and the chat are in English, Spanish and Catalan, and they follow your
browser — with a picker in the footer when it guesses wrong. A query can also
carry `language`: `en`, `es` or `ca`, and that is what the notification email
uses, because there is no browser to ask. It changes the wrapper the product
puts around your question — the subject line, the labels, the dates — and it
does **not** translate your words.
```

El resto de la entrada —lo de que la pregunta y las opciones se envían tal cual,
y la excepción del código de acceso— se queda como está.

- [ ] **Paso 3: cambia la convención en `CLAUDE.md`**

En la sección `## Conventions`, debajo de «Code, comments and commit messages in
English», añade:

```markdown
User-visible text in `web/` is the exception, and it does not live in the JSX:
it lives in `web/src/i18n/catalogues/`, in English, Spanish and Catalan. `en` is
the source and the type — a key missing from it does not compile, and a key
missing from `es` or `ca` fails `tests/unit/web-catalogue-parity.test.ts`. A new
string written by hand into a component is caught by `i18next/no-literal-string`
in `web/`'s ESLint config. What is never translated: anything an agent or a
person wrote — a question, a subject, an answer option, a message.
```

Y en `## Traps`, una entrada nueva, porque cuesta tiempo y ya ha costado el suyo:

```markdown
**The interface language has three sources and the order matters.** Chosen in
the picker (`localStorage`) beats what the agent declared on the query, which
beats the browser, and English is the floor. Only the picker persists:
`/q/:token` applies the declared language with `persist: false`, and if that
ever flips, one agent's wrong declaration becomes that person's permanent
language on that device. `tests/unit/web-language-resolution.test.ts` holds the
whole table.
```

- [ ] **Paso 4: la comprobación final, entera**

```bash
bun test tests/unit tests/integration
bunx tsc --noEmit
bunx biome check src/
cd web && bun install && bunx tsc -b && bunx eslint . && bun run build
```

El `bun run build` de `web/` no es opcional: regenera
`web/public/agentdialog-integration-guide.md` desde `docs/api/README.md`. Si sale
modificado, va en el commit — es una trampa documentada del repo y ya se ha
colado una vez.

- [ ] **Paso 5: commit y PR**

```bash
git add .github/workflows/ci.yml docs-site/content/docs/roadmap.mdx CLAUDE.md web/public
git commit -m "Check web in CI, and write down what the language rules are"
git push -u origin <rama>
gh pr create --title "The interface in three languages" --body "…"
```

El cuerpo del PR: qué se traduce, qué no, la tabla de precedencia, y que no toca
el backend.

---

## Lo que este plan deja fuera a propósito

- **`humans.preferences`** y que la elección viaje entre dispositivos.
- **El euskera**, hasta que haya quien revise el catálogo.
- **La revisión del catalán** por alguien que lo hable, que sigue pendiente
  también de `src/i18n/ca.ts` desde la primera pieza. No bloquea fusionar; sí
  bloquea decir en público que el producto está en catalán.
- **URLs por idioma, `hreflang` y el `<title>` traducido.** La landing es una SPA
  sin prerender y un rastreador solo ve inglés. Es un proyecto aparte.
- **Vitest y Testing Library en `web/`.** Lo cubren el tipado de claves, la
  paridad y la regla del linter. Si hace falta un runner ahí, que llegue con un
  caso que lo pida.
